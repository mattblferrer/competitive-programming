#include <bits/stdc++.h>

int ABC119C() {
    int n, a, b, c, mp = 0;
    std::cin >> n >> a >> b >> c;
    std::vector<int> l(n);
    for (int i = 0; i < n; i++) {
        std::cin >> l[i];
    }
    // TODO: solve
}

int main() {
    std::cout << ABC119C();
    return 0;
}
