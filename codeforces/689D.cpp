#include <bits/stdc++.h>

long long problem689D() {
    int n;
    std::cin >> n;
    std::vector<int> a(n), b(n);
    for (int i = 0; i < n; i++) {
        std::cin >> a[i];
    }
    for (int i = 0; i < n; i++) {
        std::cin >> b[i];
    }
    long long q = 0;
    // TODO: solve
    return q;
}

int main() {
    std::cout << problem689D();	
    return 0;
}