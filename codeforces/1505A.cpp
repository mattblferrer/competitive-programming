#include <bits/stdc++.h>

void problem1505A() {
    std::string q;
    while (std::getline(std::cin, q)) {
        std::cout << "NO\n";
        std::cout << std::flush;
    }
}

int main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);

    problem1505A();
    return 0;
}