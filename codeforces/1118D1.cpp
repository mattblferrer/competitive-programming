#include <bits/stdc++.h>

void problem1118D1() {
    int n, m;
    std::cin >> n >> m;
    std::vector<int> a(n);
    for (int i = 0; i < n; i++) {
        std::cin >> a[i];
    }
    // TODO: solve
}

int main() {
    problem1118D1();
    return 0;
}