#include <iostream>
#include <map> 

void ABC170E() {
    long long n, q, a_i, b_i, c_j, d_j;
    std::cin >> n >> q;

    for (int i = 0; i < n; i++) {
        std::cin >> a_i >> b_i;
    }
    for (int j = 0; j < q; j++) {
        std::cin >> c_j >> d_j;
    }
    // TODO: solve as homework
}   

int main() { 
    ABC170E();
    return 0;
}