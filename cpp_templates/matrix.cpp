#include <bits/stdc++.h>

class Matrix {
    public:
    std::vector<std::vector<long long>> a;

    Matrix(int n, int m) {
        
    }
    Matrix(std::vector<std::vector<long long>> a) {
        
    }
};